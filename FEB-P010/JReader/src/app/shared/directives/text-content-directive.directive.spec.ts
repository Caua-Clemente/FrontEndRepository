import { TextContentDirectiveDirective } from './text-content-directive.directive';

describe('TextContentDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new TextContentDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
