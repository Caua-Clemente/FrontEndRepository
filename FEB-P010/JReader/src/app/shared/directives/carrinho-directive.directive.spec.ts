import { CarrinhoDirectiveDirective } from './carrinho-directive.directive';

describe('CarrinhoDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CarrinhoDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
