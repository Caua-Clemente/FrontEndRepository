import { TextTitleDirectiveDirective } from './text-title-directive.directive';

describe('TextTitleDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new TextTitleDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
