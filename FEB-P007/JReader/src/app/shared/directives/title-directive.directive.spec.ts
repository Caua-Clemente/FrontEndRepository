import { TitleDirectiveDirective } from './title-directive.directive';

describe('TitleDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new TitleDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
