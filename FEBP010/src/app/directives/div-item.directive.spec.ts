import { DivItemDirective } from './div-item.directive';

describe('DivItemDirective', () => {
  it('should create an instance', () => {
    const directive = new DivItemDirective();
    expect(directive).toBeTruthy();
  });
});
